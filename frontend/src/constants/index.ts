const API = {
  CATEGORY_GET_SIZE: 4,
};

const CACHE_KEY = {
  SCHEDULES: 'schedules',
  CATEGORIES: 'categories',
};

const PATH = {
  CALENDAR_PAGE: '/',
  START_PAGE: '/start',
};

export { API, CACHE_KEY, PATH };
