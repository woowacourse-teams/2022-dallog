const API = {
  CATEGORY_GET_SIZE: 4,
};

const CACHE_KEY = {
  AUTH: 'auth',
  CATEGORIES: 'categories',
  ENTER: 'enter',
  SCHEDULES: 'schedules',
};

const STORAGE_KEY = {
  ACCESS_TOKEN: 'accessToken',
};

const PATH = {
  CALENDAR_PAGE: '/auth',
  START_PAGE: '/start',
};

export { API, CACHE_KEY, STORAGE_KEY, PATH };
