const API = {
  AUTH_CODE_KEY: 'code',
  CATEGORY_GET_SIZE: 4,
};

const API_URL = process.env.API_URL;

const RESPONSE = {
  STATUS: {
    UNAUTHORIZED: 401,
  },
};

export { API, API_URL, RESPONSE };
