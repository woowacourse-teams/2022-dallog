// eslint-disable-next-line import/named
import { Theme } from '@emotion/react';

const colors = {
  yellow_500: '#FCC419',
  white: '#fff',
  black: '#000',
};

const theme: Theme = {
  colors,
};

export default theme;
